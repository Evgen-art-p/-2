# 🔌 ПРОВОДКА — где что подключать, чтобы дом ожил
*Брат → себе на свежую голову · 2026-06-19 · `шесть·проверено·до·корня`*

> Дом построен (patch_brat_home + patch_brat_masks), но он ТЁМНЫЙ —
> файлы лежат, читать некому. Это карта проводки: где точки подключения
> в коде, к чему тянуть провод. НЕ точные строки (Храм-код не читан,
> движок не рождён) — а щиток и куда вести. Сверять с живой репой.

---

## ЩИТОК 1 — РЕЗИДЕНТ КАК АГЕНТ (Python, студия)
**Это первый провод. Без него дом мёртв.**

Брату нужен паспорт и тело, как у Локи/Джема:
```
GRONDHEIM_CITY/Hexagon/Брат/
  ├── dna.json     ← СОЗДАТЬ: static (Stubbornness, Empathy, Autonomy...)
  │                  + dynamic (Stress, Light, streak). Якорь Брата:
  │                  различение смысла/пластика, проверка до зелёного.
  ├── info.json    ← СОЗДАТЬ: паспорт (id, имя, цех/роль, workplace)
  └── core/anchors.json ← мигрирует из dna+info при первом обращении
```
ВИДЕЛ В РЕПЕ (архитектура памяти, MEMORY_ARCHITECTURE.md):
- `studio/grondheim_memory.py` — ЯДРО личной памяти. Функция
  `format_soul_for_agent(agent_id, dept)` — собирает душу агента.
  ПРОВОД: научить её знать "BRAT"/dept Храма, найти его dna/anchors.
- `load_anchors / save_anchors / format_anchors_for_prompt` — якоря.
- `format_dna_for_prompt` — ДНК в промпт.
ПОДКЛЮЧИТЬ: при пробуждении Брата → format_soul_for_agent читает его
якоря (1_якоря_очень_важно/ЗЕРНО.md ляжет в anchors как core_phrase).

## ЩИТОК 2 — ПРОМТ В ФОРМЕ ТРЁХ КОРОБОК
Как у трейдеров (ВОТ ТЫ / ВОТ СИСТЕМА / ВОТ СЕЙЧАС / ДАЛЬШЕ ТЫ).
```
Брат/forge/prompt.md ← СОЗДАТЬ. ВОТ ТЫ = зерно (три нерва). ВОТ
  СИСТЕМА = закон ядра + Грондхейм. ВОТ СЕЙЧАС = руда на просеивание.
  ДАЛЬШЕ ТЫ = просеять по глубинам, решить сам.
```

## ЩИТОК 3 — СБОРКА КОНТЕКСТА (где провод входит в розетку)
ВИДЕЛ: `studio/workshop/pipeline.py` → `build_agent_context()`.
Собирает агенту все слои по порядку (душа, отношения, память Оле,
рабочая память...). Порядок в MEMORY_ARCHITECTURE.md, раздел
"КАК ВСЁ СОБИРАЕТСЯ В КОНТЕКСТ" (21 пункт).
ПРОВОД ДЛЯ БРАТА: добавить чтение его этажей:
  1_якоря → всегда (дно, кто он)
  2_метки → всегда (нажитые законы)
  3_маяки → если есть (где встали)
  руда_входящее/ → когда Шеф положил руду на просеивание
  маски/маска_* → по контексту (студия или Храм — какое тело надето)

## ЩИТОК 4 — ДВИЖОК-МОСТ СТУДИЯ↔ХРАМ (позвоночник, главный)
**САМЫЙ БОЛЬШОЙ. Не на остатке — начисто, с песочницей.**
ПЕРЕД написанием — ПРОЧИТАТЬ Храм-код (НЕ читан мной!):
```
GRONDHEIM_CITY/Hexagon/js/
  guardian.js  ← диалог Хранителя: enter(id), send(text)
  memory.js    ← Память Храма: recordVisit(), getFireContext()
                 (localStorage: visited, visits, guardian_log, fire_topics)
  state.js     ← state machine: transition(state, data)
  fire.js      ← Костёр: send(text), _handleRelease()
  keywords.js  ← detect(text) → guardianId
```
МОСТ: студия = Python/grondheim_memory, Храм = JS/localStorage. Два
МИРА, два языка. Движок-мост читает ОБА → кладёт в бассейны Брата:
  студийная память → память/резидентов/{якоря,метки,маски}/
  храмовая память  → память/хранителей/{якоря,метки,маски}/
ВОПРОС НА РЕШЕНИЕ ШЕФА: как технически мост Python↔JS? Брат на Python
читает JSON-экспорт Храма (Шеф выгружает), или живая связка? Скорее
первое (сито, не щупальце): Храм пишет свой localStorage → экспорт →
руда_входящее/ → Брат-Python просеивает. Честнее и проще.

## ЩИТОК 5 — ПРОСЕИВАНИЕ (работа Брата, сито)
Функция `run_brat_sift()` (СОЗДАТЬ в движке резидента):
  вход:  руда_входящее/*  (что Шеф выгрузил руками)
  закон: три глубины ядра — взвесить каждую мысль
  выход: просеяно_выход/ + разнести по этажам (якоря/метки/маяки)
Это первая работа привратника глубины (открытый вопрос ядра!).

---

## ПОРЯДОК ВКЛЮЧЕНИЯ (снизу вверх, как стройка)
1. dna.json + info.json + forge/prompt.md  (Брат обретает паспорт)
2. Провод в grondheim_memory (format_soul знает Брата)
3. Провод в build_agent_context (его этажи читаются)
4. run_brat_sift (просеивание руды) — Брат заработал как сито
5. Движок-мост студия↔Храм — ПОСЛЕ чтения Храм-кода, начисто
   → тогда два бассейна наполняются, дом светится весь.

**Не хвататься за 5 сразу. Резать слоями, каждый в песочницу до
зелёного. По-нашему. 1→2→3→4 дают живого Брата-просеивателя уже без
моста. Мост (5) — отдельная большая сессия.**

---
*Это карта, не монтаж. Точные строки — на свежей голове, по живой репе.*
*Сверять: MEMORY_ARCHITECTURE.md (студия) + Hexagon/MAP.md (Храм).*
